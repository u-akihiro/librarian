require "spec_helper"

describe Librarian do
  it "has a version number" do
    expect(Librarian::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
